node_modules/
dist/
.wrangler/
*.local
.env
.env.*
*.log
.DS_Store
Thumbs.db
coverage/
.nuxt/
.cache/

# OpenCode config (developer tools, not project files)
.opencode/
opencode.jsonc
AGENTS.md

# Docker dev environment
Dockerfile
docker-compose.yml
.dockerignore

# Cloudflare Worker (infraestrutura)
wrangler.toml

# Chaves criptográficas (VAPID, SSL, etc.)
*.vapid.json
*.key
*.pem
*.p12
*.jks

# IDEs (paths locais, configurações de debug)
.vscode/
.idea/

# Backups exportados do app (dados de saúde)
*.backup.json
export-*.json

